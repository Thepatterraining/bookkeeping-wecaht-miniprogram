<view class="bill-list">
  <!-- 账单列表 -->
  <block wx:if="{{loading}}">
    <view class="loading">
      <view class="loading-spinner"></view>
      <text>加载中...</text>
    </view>
  </block>
  <block wx:elif="{{error}}">
    <view class="error">
      <icon type="warn" size="40" color="#ff4d4f"></icon>
      <text>{{error}}</text>
    </view>
  </block>
  <block wx:elif="{{bills.length === 0}}">
    <view class="empty">
      <icon type="info" size="40" color="#999"></icon>
      <text>暂无账单</text>
    </view>
  </block>
  <block wx:else>
    <view class="bill-items">
      <block wx:for="{{bills}}" wx:key="no">
        <!-- 滑动删除容器 -->
        <view class="swipe-area">
          <movable-area style="width: 100%; height: 140rpx;">
            <movable-view class="swipe-item" direction="horizontal" x="{{item.slideX}}"
              out-of-bounds="true" damping="20" friction="2" disabled="{{false}}"
              bindtouchstart="handleTouchStart" bindtouchend="handleTouchEnd"
              data-index="{{index}}" data-no="{{item.no}}" style="width: calc(100% + 150rpx);">
              <!-- 账单内容 -->
              <view class="bill-item {{item.amount < 0 ? 'income' : 'expense'}}" bindtap="onTapItem" data-no="{{item.no}}">
                <view class="bill-left">
                  <view class="icon">{{item.categoryIcon || '📝'}}</view>
                  <view class="meta">
                    <text class="category">{{item.categoryName}}</text>
                    <text class="desc">{{item.desc}}</text>
                    <text class="time">{{formatTime(item.time)}}</text>
                  </view>
                </view>
                <view class="bill-right">
                  <text class="amount {{item.amount < 0 ? 'income-text' : 'expense-text'}}">{{item.amount < 0 ? '+' : '-'}}¥{{formatAmount(Math.abs(item.amount))}}</text>
                </view>
              </view>
              <!-- 删除按钮 -->
              <view class="delete-btn" bindtap="deleteBill" data-no="{{item.no}}">删除</view>
            </movable-view>
          </movable-area>
        </view>
      </block>
    </view>
  </block>
</view> 